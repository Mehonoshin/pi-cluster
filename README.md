### Installation

Install sshpass to run the first time with password `https://github.com/hudochenkov/homebrew-sshpass`

Install external roles with command: `ansible-galaxy install -r requirements.yml`
